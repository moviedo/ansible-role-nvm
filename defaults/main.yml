---

nvm_version: "v0.35.2"
nvm_node_version: "12.14.1"
nvm_user: vagrant

required_packages:
  - git
  - curl
  - libssl-dev

nvm_working_path: "/home/{{ nvm_user }}"
nvm_dest: "{{ nvm_working_path }}/.nvm"
