---

nvm_version: "v0.35.2"
nvm_node_version: "12.14.1"
nvm_user: vagrant
# define if group is different from user
#nvm_group:

shared_packages:
  - git
  - curl

debian_packages:
  - libssl-dev

redhat_packages:
  - openssl-devel

nvm_working_path: "~"
nvm_dest: "~/.nvm"
